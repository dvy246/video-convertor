---
import BaseLayout from './BaseLayout.astro';
import { ConverterApp } from '../components/converter/ConverterApp';
import HowItWorks from '../components/sections/HowItWorks.astro';
import FAQSection from '../components/sections/FAQSection.astro';
import FormatGrid from '../components/sections/FormatGrid.astro';
import type { FormatPageData } from '../data/formatPages';
import type { SupportedLanguage } from '../i18n/languages';
import { getLangFromUrl, useTranslations, useTranslatedPath } from '../i18n/utils';
import { 
  generateWebApplicationSchema, 
  generateHowToSchema, 
  generateFAQSchema, 
  generateBreadcrumbSchema
} from '../lib/seo/schema';

export interface Props {
  data: FormatPageData;
  lang?: SupportedLanguage;
}

const { data, lang } = Astro.props;
const currentLang = lang || getLangFromUrl(Astro.url);
const t = useTranslations(currentLang);
const translatePath = useTranslatedPath(currentLang);
const canonicalUrl = `https://conversordevideo.com${translatePath(`/${data.slug}`)}`;

const webAppSchema = generateWebApplicationSchema({
  name: `${data.h1Prefix} ${data.h1Highlight}`,
  description: data.metaDescription,
  url: canonicalUrl,
  applicationSubCategory: data.targetFormat === 'mp3' ? 'Audio Converter' : undefined,
  lang: currentLang
});

const howToSchema = generateHowToSchema({
  name: `${data.h1Prefix} ${data.h1Highlight}`,
  description: data.heroSubtitle,
  lang: currentLang,
  steps: [
    {
      position: 1,
      name: t('howItWorks.step1Title'),
      text: t('howItWorks.step1Desc')
    },
    {
      position: 2,
      name: t('howItWorks.step2Title'),
      text: t('howItWorks.step2Desc')
    },
    {
      position: 3,
      name: t('howItWorks.step3Title'),
      text: t('howItWorks.step3Desc')
    }
  ]
});

const homeLabel = currentLang === 'pt' ? 'Início' : (currentLang === 'es' ? 'Inicio' : (currentLang === 'fr' ? 'Accueil' : (currentLang === 'ja' ? 'ホーム' : (currentLang === 'zh' ? '首页' : (currentLang === 'no' ? 'Hjem' : (currentLang === 'tr' ? 'Ana Sayfa' : (currentLang === 'pl' ? 'Strona Główna' : 'Home')))))));

const breadcrumbSchema = generateBreadcrumbSchema([
  { name: homeLabel, url: `https://conversordevideo.com${translatePath('/')}` },
  { name: `${data.h1Prefix} ${data.h1Highlight}`, url: canonicalUrl }
]);

const faqSchema = generateFAQSchema(data.faqs);
---

<BaseLayout
  title={data.metaTitle}
  description={data.metaDescription}
  canonicalUrl={canonicalUrl}
  keywords={data.keywords}
  lang={currentLang}
  schemas={[webAppSchema, howToSchema, faqSchema, breadcrumbSchema]}
>
  <!-- Hero Section -->
  <section class="pt-8 pb-12 sm:pt-14 sm:pb-16 px-4 sm:px-6">
    <div class="max-w-4xl mx-auto text-center space-y-4 mb-8">
      <div class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full bg-emerald-50 dark:bg-emerald-950/60 text-emerald-800 dark:text-emerald-300 text-xs font-bold uppercase tracking-wider border border-emerald-200/80 dark:border-emerald-800/60">
        {data.badge}
      </div>
      <h1 class="text-3xl sm:text-5xl font-black text-zinc-900 dark:text-white tracking-tight leading-tight">
        {data.h1Prefix} <span class="relative inline-block whitespace-nowrap"><span class="text-transparent bg-clip-text bg-gradient-to-r from-emerald-600 via-teal-500 to-emerald-600 dark:from-emerald-400 dark:via-teal-300 dark:to-emerald-400 drop-shadow-2xs">{data.h1Highlight}</span><svg class="absolute -bottom-1 sm:-bottom-2 left-0 w-full h-2 sm:h-2.5 text-emerald-500 dark:text-emerald-400 opacity-80 pointer-events-none select-none overflow-visible" viewBox="0 0 100 8" fill="none" preserveAspectRatio="none" aria-hidden="true"><path d="M0 6C30 1 70 1 100 6" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" /></svg></span> {data.h1Suffix}
      </h1>
      <p class="text-base sm:text-lg text-zinc-600 dark:text-zinc-400 max-w-2xl mx-auto leading-relaxed">
        {data.heroSubtitle}
      </p>
    </div>

    <!-- Converter Island -->
    <ConverterApp 
      initialTargetFormat={data.targetFormat}
      initialPreset={data.initialPreset}
      defaultTitle={data.dropzoneTitle}
      lang={currentLang}
      client:idle 
    />
  </section>

  <!-- How It Works Section -->
  <HowItWorks 
    title={`${data.h1Prefix} ${data.h1Highlight}`}
    subtitle={t('howItWorks.subtitle')}
    lang={currentLang}
  />

  <!-- Technical Explainer Section -->
  {data.explainerParagraphs && data.explainerParagraphs.length > 0 && (
    <section class="py-12 sm:py-16 bg-white dark:bg-zinc-900 border-y border-zinc-200/80 dark:border-zinc-800 transition-colors">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 space-y-8 text-zinc-700 dark:text-zinc-300">
        <div class="space-y-3">
          <h2 class="text-2xl sm:text-3xl font-extrabold text-zinc-900 dark:text-white tracking-tight">
            {data.explainerTitle}
          </h2>
          {data.explainerParagraphs.map((p) => (
            <p class="text-sm sm:text-base leading-relaxed text-zinc-600 dark:text-zinc-400">
              {p}
            </p>
          ))}
        </div>

        {data.proTip && (
          <div class="rounded-2xl bg-amber-50/80 dark:bg-amber-950/40 border border-amber-200/80 dark:border-amber-800/60 p-5 sm:p-6 space-y-2">
            <div class="flex items-center gap-2 text-amber-800 dark:text-amber-300 font-bold text-sm sm:text-base">
              <span>💡</span>
              <span>
                {currentLang === 'es' ? 'Consejo Pro de Optimización' :
                 currentLang === 'en' ? 'Pro Optimization Tip' :
                 currentLang === 'fr' ? 'Conseil Pro d’Optimisation' :
                 currentLang === 'ja' ? '最適化のプロのヒント' :
                 currentLang === 'zh' ? '专业优化技巧' :
                 currentLang === 'no' ? 'Pro Optimaliseringstips' :
                 currentLang === 'tr' ? 'Profesyonel Optimizasyon İpucu' :
                 currentLang === 'pl' ? 'Wskazówka Dotycząca Optymalizacji' :
                 'Dica Pro de Otimização'}
              </span>
            </div>
            <p class="text-xs sm:text-sm text-amber-900/90 dark:text-amber-200/90 leading-relaxed">
              {data.proTip}
            </p>
          </div>
        )}

        {(data.sourceDetails || data.targetDetails) && (
          <div class="grid grid-cols-1 sm:grid-cols-2 gap-6 pt-2">
            {data.sourceDetails && (
              <div class="bg-zinc-50 dark:bg-zinc-800/60 border border-zinc-200 dark:border-zinc-700/80 rounded-2xl p-6 space-y-3">
                <h3 class="font-bold text-base text-zinc-900 dark:text-white">
                  {data.sourceDetailsTitle}
                </h3>
                <ul class="text-xs sm:text-sm text-zinc-600 dark:text-zinc-400 space-y-2 list-disc pl-4">
                  {data.sourceDetails.map((item) => (
                    <li>{item}</li>
                  ))}
                </ul>
              </div>
            )}

            {data.targetDetails && (
              <div class="bg-emerald-50/50 dark:bg-emerald-950/40 border border-emerald-200 dark:border-emerald-800/60 rounded-2xl p-6 space-y-3">
                <h3 class="font-bold text-base text-emerald-950 dark:text-emerald-300">
                  {data.targetDetailsTitle}
                </h3>
                <ul class="text-xs sm:text-sm text-emerald-900 dark:text-emerald-400 space-y-2 list-disc pl-4">
                  {data.targetDetails.map((item) => (
                    <li>{item}</li>
                  ))}
                </ul>
              </div>
            )}
          </div>
        )}
      </div>
    </section>
  )}

  <!-- Format Grid Tools -->
  <FormatGrid lang={currentLang} />

  <!-- FAQ Section -->
  {data.faqs && data.faqs.length > 0 && (
    <FAQSection faqs={data.faqs} lang={currentLang} />
  )}
</BaseLayout>
